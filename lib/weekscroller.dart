import "package:flutter/material.dart";

/// TODO: Implement own week scroller because I am
/// not certain that the scroller we use right now
/// is cross compatible with ios.

/*
class WeekScroller extends Column {
  final textStyleH = TextStyle(fontSize: 16);
  final textStyleW = TextStyle(fontSize: 10);
  DateTime currentDate = DateTime.now();
  int weekIter;

  WeekScroller() {
    var children = [
      Row(
        children: [
          Text(this.currentDate.month.toString(), style: textStyleH),
          Text(this.weekIter.toString(), style: textStyleW)
        ],
        mainAxisAlignment: MainAxisAlignment.spaceBetween,
      ),
    ];

    super(children: children);
  }
}
*/
